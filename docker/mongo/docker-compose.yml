version: '3.1'

services:

  mongo:
    image: mongo
    restart: always
    #environment:
    #  MONGO_INITDB_ROOT_USERNAME: bodoque
    #  MONGO_INITDB_ROOT_PASSWORD: lol123
    ports:
      - 27017:27017